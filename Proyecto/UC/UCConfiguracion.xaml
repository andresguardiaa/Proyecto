<UserControl x:Class="Proyecto.UC.UCConfiguracion"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Proyecto.UC"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <materialDesign:PackIcon Kind="Cog" Width="35" Height="35" Foreground="#374151" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <TextBlock Text="Configuración del Sistema" FontSize="28" FontWeight="Bold" Foreground="#374151" VerticalAlignment="Center"/>
        </StackPanel>

        <TabControl Grid.Row="1" 
                    Style="{StaticResource MaterialDesignTabControl}" 
                    materialDesign:ColorZoneAssist.Mode="Standard">

            <TabItem Padding="30,15" Height="50">
                <TabItem.Header>
                    <TextBlock Text="Tablas Maestras" FontSize="18" FontWeight="SemiBold"/>
                </TabItem.Header>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Gestiona los datos base de la aplicación" FontSize="16" Foreground="Gray" Margin="0,0,0,20"/>

                        <UniformGrid Grid.Row="1" Columns="3" Rows="2">
                            <materialDesign:Card Margin="10" Padding="20" UniformCornerRadius="8">
                                <StackPanel>
                                    <materialDesign:PackIcon Kind="Tractor" Width="40" Height="40" Foreground="#3B82F6" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Modelos de Máquinas" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                                    <Button x:Name="btnGestionarModelos" Content="Gestionar" Style="{StaticResource MaterialDesignOutlinedButton}" Click="btnGestionarModelos_Click"/>
                                </StackPanel>
                            </materialDesign:Card>

                            <materialDesign:Card Margin="10" Padding="20" UniformCornerRadius="8">
                                <StackPanel>
                                    <materialDesign:PackIcon Kind="TrafficLight" Width="40" Height="40" Foreground="#14B8A6" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Estados" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                                    <Button x:Name="btnGestionarEstados" Content="Gestionar" Style="{StaticResource MaterialDesignOutlinedButton}" />
                                </StackPanel>
                            </materialDesign:Card>

                            <materialDesign:Card Margin="10" Padding="20" UniformCornerRadius="8">
                                <StackPanel>
                                    <materialDesign:PackIcon Kind="CashMultiple" Width="40" Height="40" Foreground="#F59E0B" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Tipos de Gastos" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10,0,10"/>
                                    <Button x:Name="btnGestionarGastos" Content="Gestionar" Style="{StaticResource MaterialDesignOutlinedButton}" />
                                </StackPanel>
                            </materialDesign:Card>

                        </UniformGrid>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <TabItem Padding="30,15" Height="50">
                <TabItem.Header>
                    <TextBlock Text="Preferencias" FontSize="18" FontWeight="SemiBold"/>
                </TabItem.Header>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="15">
                        <TextBlock Text="Ajustes de comportamiento y seguridad" FontSize="16" Foreground="Gray" Margin="0,0,0,20"/>

                        <materialDesign:Card Margin="0,0,0,20" Padding="20" UniformCornerRadius="8">
                            <StackPanel>
                                <TextBlock Text="Alertas y Avisos" FontSize="18" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,15"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Avisar cuando un proyecto finalice en menos de:" VerticalAlignment="Center" FontSize="15" Margin="0,0,15,0"/>
                                    <TextBox Width="50" Text="30" TextAlignment="Center" FontSize="15" Style="{StaticResource MaterialDesignOutlinedTextBox}" materialDesign:HintAssist.Hint="Días"/>
                                    <TextBlock Text="días." VerticalAlignment="Center" FontSize="15" Margin="15,0,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </materialDesign:Card>

                        <materialDesign:Card Margin="0,0,0,20" Padding="20" UniformCornerRadius="8">
                            <StackPanel>
                                <TextBlock Text="Seguridad y Datos" FontSize="18" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,15"/>
                                <TextBlock Text="Genera un volcado completo de la base de datos para evitar pérdida de información." FontSize="14" Foreground="Gray" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                <Button x:Name="btnBackup" Content="Generar Copia de Seguridad" HorizontalAlignment="Left" Background="#10B981" BorderBrush="Transparent" Foreground="White">
                                    <Button.ContentTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <materialDesign:PackIcon Kind="DatabaseExport" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Generar Copia de Seguridad" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </Button.ContentTemplate>
                                </Button>
                            </StackPanel>
                        </materialDesign:Card>

                        <Button Content="Guardar Preferencias" x:Name="btnGuardarPreferencias" HorizontalAlignment="Right" Width="200" Background="#374151" Foreground="White" BorderBrush="Transparent" Margin="0,20,0,0"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>

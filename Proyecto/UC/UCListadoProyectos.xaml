<UserControl x:Class="Proyecto.UC.UCListadoProyectos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"        
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Proyecto.UC"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="listadoProyectos"
             Loaded="listadoProyectos_Loaded">
    <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="20,10,20,10">
            <StackPanel>
                <TextBlock Text="Listado de Proyectos" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button HorizontalAlignment="Right" VerticalAlignment="Center" 
                Style="{StaticResource MaterialDesignToolButton}" 
                ToolTip="Refrescar lista" 
                x:Name="btnRefrescar"
                Click="btnRefrescar_Click" 
                Foreground="#374151">
                    <materialDesign:PackIcon Kind="Refresh" Width="25" Height="25"/>
                </Button>

                <!-- <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock Text="Ver desde:" VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <DatePicker SelectedDate="{Binding FechaFacturaFiltro}" Width="150" Margin="0,0,10,0" />

                    <Button Content="Filtrar" Background="#374151" Foreground="White" BorderBrush="Transparent" x:Name="btnFiltrar" Click="btnFiltrar_Click" Margin="0,0,10,0" Width="80"/>
                    <Button Content="Limpiar" BorderBrush="#374151" Foreground="#374151" x:Name="btnLimpiar" Click="btnLimpiar_Click" Style="{StaticResource MaterialDesignOutlinedButton}" Width="80"/>
                </StackPanel>-->
            </StackPanel>
        </Grid>
        <Grid>
            <DataGrid ItemsSource="{Binding ListaProyectos}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  ColumnWidth="*"
                  SelectedItem="{Binding ProyectoSeleccionado}">
                
                
                
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Proyecto" Binding="{Binding Nombre}" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="Cliente">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Nombre: " FontWeight="Bold"/>
                                        <TextBlock Text="{Binding ClienteIdClienteNavigation.Nombre}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Teléfono: " FontWeight="Bold"/>
                                        <TextBlock Text="{Binding ClienteIdClienteNavigation.Telefono}"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Duración">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding FechaInicio, StringFormat={}{0:dd/MM/yyyy}}"/>
                                    <TextBlock Text=" - "/>
                                    <TextBlock Text="{Binding FechaFin, StringFormat={}{0:dd/MM/yyyy}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Editar">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button x:Name="btnEditarProyecto" Click="btnEditarProyecto_Click" MaxWidth="200" Foreground="White" BorderBrush="Transparent">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Editar" Margin="10,0,10,0"/>
                                        <materialDesign:PackIcon Kind="Edit"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Finalizar">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button x:Name="btnEliminarProyecto" Click="btnEliminarProyecto_Click" MaxWidth="200" Foreground="White" BorderBrush="Transparent" Background="#f54e42">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Finalizar Proyecto" Margin="10,0,10,0"/>
                                        <materialDesign:PackIcon Kind="CloseBold" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</UserControl>

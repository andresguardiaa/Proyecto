<UserControl x:Class="Proyecto.UC.UCListadoMaquinas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Proyecto.UC"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="listadoMaquinas"
             Loaded="listadoMaquinas_Loaded">
    <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="20,10,20,10">
            <StackPanel>
                <TextBlock Text="Listado de Máquinas" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button HorizontalAlignment="Right" VerticalAlignment="Center" 
                    Style="{StaticResource MaterialDesignToolButton}" 
                    ToolTip="Refrescar lista" 
                    x:Name="btnRefrescar"
                    Click="btnRefrescar_Click" 
                    Foreground="#374151">
                    <materialDesign:PackIcon Kind="Refresh" Width="25" Height="25"/>
                </Button>

                <!-- <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                <TextBlock Text="Ver desde:" VerticalAlignment="Center" Margin="0,0,10,0"/>

                <DatePicker SelectedDate="{Binding FechaFacturaFiltro}" Width="150" Margin="0,0,10,0" />

                <Button Content="Filtrar" Background="#374151" Foreground="White" BorderBrush="Transparent" x:Name="btnFiltrar" Click="btnFiltrar_Click" Margin="0,0,10,0" Width="80"/>
                <Button Content="Limpiar" BorderBrush="#374151" Foreground="#374151" x:Name="btnLimpiar" Click="btnLimpiar_Click" Style="{StaticResource MaterialDesignOutlinedButton}" Width="80"/>
            </StackPanel>-->
            </StackPanel>
        </Grid>
        <Grid>
            <DataGrid ItemsSource="{Binding ListaMaquinas}"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          ColumnWidth="*"
          SelectedItem="{Binding MaquinaSeleccionada}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Modelo" Binding="{Binding IdModeloNavigation.ModeloMaquina}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Ubicación" Binding="{Binding Ubicacion}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Tarifa" Binding="{Binding Tarifa}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Capacidad" Binding="{Binding Capacidad}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding IdEstadoNavigation.Descripcion}" IsReadOnly="True"/>
                    
                    <DataGridTemplateColumn Header="Editar">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button x:Name="btnEditarMaquina" Click="btnEditarMaquina_Click" Foreground="White" BorderBrush="Transparent">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Editar" Margin="10,0,10,0"/>
                                        <materialDesign:PackIcon Kind="Edit"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Dar de Baja">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button x:Name="btnEliminarMaquina" Click="btnEliminarMaquina_Click" Foreground="White" BorderBrush="Transparent" Background="#f54e42">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Dar de Baja" Margin="10,0,10,0"/>
                                        <materialDesign:PackIcon Kind="Trash"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</UserControl>
